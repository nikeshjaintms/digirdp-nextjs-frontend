// Generate metadata dynamically
export async function generateMetadata() {

    return {
      title: "DigiRDP Dedicated Server",
      description: "With SSD storage, high-memory variants and latest processors, use our servers to get the best performance.",
    }
  }
  
  const Layout = async ({ children }) => {
      // Vars
    
      return <>{children}</>;
    };
    
    export default Layout;