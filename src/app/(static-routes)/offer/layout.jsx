// Type Imports

export const metadata = {
  title: "Limited-Time Deals: Save Big on RDP and VPS Services!",
  description:
    "Upgrade to high-performance RDP and VPS services without breaking the bank. Grab our limited-time promo codes and enjoy premium solutions at discounted rates.",
};

const Layout = async ({ children }) => {
  // Vars

  return <>{children}</>;
};

export default Layout;
