// Type Imports

export const metadata = {
    title: "Looking Glass || DigiRDP"
  };
  
  const Layout = async ({ children }) => {
    // Vars
  
    return <>{children}</>;
  };
  
  export default Layout;
  