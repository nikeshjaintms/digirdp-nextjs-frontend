// Type Imports

export const metadata = {
  title: "Careers at DigiRDP",
  description: `Step into a world of innovation and growth. At DigiRDP,
                    we’re looking for passionate individuals to join our
                    team and help revolutionize the cloud and VPS industry.!`,
};

const Layout = async ({ children }) => {
  // Vars

  return <>{children}</>;
};

export default Layout;
